@startuml DM_BenLQ_Create
!pragma layout smetana
title Activity Diagram: Thêm mới danh mục bên liên quan
start
:Người dùng chọn chức năng **Thêm mới danh mục bên liên quan** từ menu\n(**DM_BenLQ_Create**);
:Hệ thống hiển thị form thêm mới danh mục\n(**DM_BenLQ_Create**);
:Nhập thông tin danh mục (Mã danh mục **Tự sinh theo hệ thống**, Tên danh mục);
:Người dùng bấm nút **Lưu thông tin**;
:Hệ thống kiểm tra dữ liệu hợp lệ\n(mã danh mục không trùng lặp);
if (Dữ liệu hợp lệ?) then (Có)
  :Lưu dữ liệu danh mục;
  :Thông báo "Thêm mới danh mục bên liên quan thành công";
  :Quay về danh sách\n(**DM_BenLQ_List**);
else (Không)
  if (Nhập thiếu thông tin bắt buộc?) then (Có)
    :Hiển thị cảnh báo lỗi;
    :Ở lại màn hình\n(**DM_BenLQ_Create**);
  else (Lỗi hệ thống)
    :Hiển thị thông báo lỗi hệ thống;
    :Ở lại màn hình\n(**DM_BenLQ_Create**);
  endif
endif

if (Người dùng chọn **Hủy**) then (Có)
  :Form đóng, không lưu dữ liệu;
  :Quay về danh sách\n(**DM_BenLQ_List**);
endif

stop
@enduml
